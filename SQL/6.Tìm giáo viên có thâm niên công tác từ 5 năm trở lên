CREATE PROCEDURE sp_TimGiaoVienThamNien
AS
BEGIN
    SELECT MaGV, HoTen, NgayVaoLam, 
           DATEDIFF(YEAR, NgayVaoLam, GETDATE()) AS SoNamCongTac
    FROM GiaoVien
    WHERE DATEDIFF(YEAR, NgayVaoLam, GETDATE()) >= 5;
END;
GO
